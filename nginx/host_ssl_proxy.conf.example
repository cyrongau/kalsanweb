# /etc/nginx/sites-available/kalsanweb
# This file goes on your DigitalOcean Host (NOT inside Docker)

server {
    server_name your-domain.com www.your-domain.com; # REPLACE THIS with your actual domain

    location / {
        # Proxy to the Docker Nginx container running on port 8080
        proxy_pass http://localhost:8080;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
        
        # Forward real IP to Docker
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
